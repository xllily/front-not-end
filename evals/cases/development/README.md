# Active Development Case

Version 0.1 has one active case:

- [`existing-list-search-reuse`](existing-list-search-reuse/task.md) checks a
  product-requested project list with keyword search and stable “load more”.

Its organization context explicitly establishes a workspace data boundary and
an existing query/cursor capability. Those facts belong to this scenario and
must not become defaults for unrelated applications.
