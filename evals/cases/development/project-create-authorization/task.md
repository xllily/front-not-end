# Product request

On the projects page, let workspace administrators create a project by name.
Submitting the same create action again after a double-click or network retry
must not create a duplicate. Other workspace members must not be able to create
projects, and the existing project-details experience must keep working.
